//this is a header line, which tells standard input/output of C compile to allow use of printf command
#include <stdio.h>  

//here we use main function of C as "main"
int main (void) 
{
 //this line we print hello world using command printf. also, we always give semi-colon after every lines of code
    printf ("hello world!!"); 
}
